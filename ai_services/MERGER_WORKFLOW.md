# Dataset Merger - Visual Workflow

## 📊 Complete Pipeline Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PLANT DISEASE DATASET MERGER                      │
│                         Complete Pipeline                            │
└─────────────────────────────────────────────────────────────────────┘

INPUT: Multiple Dataset Folders
│
│  myDatasets/
│  ├── BPLD Dataset/
│  ├── Crops/
│  ├── Rice Dataset/
│  ├── Sugarcane Leaf Disease Dataset/
│  └── ... (more datasets)
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│  STEP 1: DATASET SCANNING                                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                       │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐            │
│  │  Recursive  │───▶│  Extract    │───▶│  Collect    │            │
│  │  File Scan  │    │  Class      │    │  Metadata   │            │
│  └─────────────┘    │  Labels     │    └─────────────┘            │
│                     └─────────────┘                                 │
│                                                                       │
│  For each image:                                                     │
│  • Find all image files (.jpg, .png, .bmp, .tiff)                   │
│  • Extract class from directory structure                            │
│  • Read image properties (resolution, format, mode)                  │
│  • Compute MD5 hash (exact duplicate detection)                      │
│  • Compute perceptual hash (near-duplicate detection)                │
│  • Extract EXIF data (camera, date, GPS if available)               │
│  • Calculate quality score (sharpness via Laplacian)                 │
│  • Store complete provenance (dataset, path, filename)              │
│                                                                       │
│  Output: image_registry[] - List of all images with metadata        │
└─────────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│  STEP 2: DEDUPLICATION                                              │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │  A. Build Hash Maps                                      │       │
│  │  ───────────────────                                      │       │
│  │  MD5 Hash Map:     {hash: [img1, img2, ...]}            │       │
│  │  Perceptual Map:   {phash: [img1, img2, ...]}           │       │
│  └──────────────────────────────────────────────────────────┘       │
│                              │                                       │
│                              ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │  B. Find Exact Duplicates (MD5)                          │       │
│  │  ────────────────────────────                             │       │
│  │  • Group images with identical MD5 hashes                │       │
│  │  • These are bit-for-bit identical files                 │       │
│  └──────────────────────────────────────────────────────────┘       │
│                              │                                       │
│                              ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │  C. Find Near Duplicates (Perceptual Hash)               │       │
│  │  ───────────────────────────────────────                  │       │
│  │  • Group images with similar perceptual hashes           │       │
│  │  • Detects cropped, resized, or modified versions        │       │
│  └──────────────────────────────────────────────────────────┘       │
│                              │                                       │
│                              ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │  D. Quality-Based Selection                              │       │
│  │  ────────────────────────                                 │       │
│  │  For each duplicate group:                               │       │
│  │  1. Sort by quality_score (Laplacian variance)           │       │
│  │  2. Keep highest quality version                         │       │
│  │  3. Merge provenance from all duplicates                 │       │
│  │  4. Mark others as is_duplicate=True                     │       │
│  └──────────────────────────────────────────────────────────┘       │
│                                                                       │
│  Output: Updated image_registry with duplicate flags                │
└─────────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│  STEP 3: SIMILARITY CLUSTERING                                      │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                       │
│  Purpose: Group similar images to prevent data leakage              │
│           (keeps same plant/leaf across splits together)             │
│                                                                       │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐            │
│  │  Extract    │───▶│  Compute    │───▶│  DBSCAN     │            │
│  │  Perceptual │    │  Distance   │    │  Clustering │            │
│  │  Hashes     │    │  Matrix     │    └─────────────┘            │
│  └─────────────┘    └─────────────┘           │                    │
│                                                 ▼                    │
│  Algorithm: DBSCAN                    ┌─────────────────┐           │
│  • eps = 5.0 (Hamming distance)       │ Assign Cluster  │           │
│  • min_samples = 2                    │ IDs to Images   │           │
│  • metric = precomputed               └─────────────────┘           │
│                                                                       │
│  Result: Similar images get same cluster_id                         │
│         (used later to keep clusters together in splits)             │
│                                                                       │
│  Output: similarity_clusters{} - Cluster assignments                │
└─────────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│  STEP 4: STRATIFIED SPLITTING                                       │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                       │
│  Input: Unique images (is_duplicate=False)                          │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │  A. Group by Class                                        │       │
│  │  ──────────────────                                       │       │
│  │  class_groups = {                                         │       │
│  │    'healthy': [img1, img2, ...],                         │       │
│  │    'early blight': [img1, img2, ...],                    │       │
│  │    'late blight': [img1, img2, ...],                     │       │
│  │    ...                                                    │       │
│  │  }                                                        │       │
│  └──────────────────────────────────────────────────────────┘       │
│                              │                                       │
│                              ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │  B. For Each Class:                                       │       │
│  │  ────────────────                                         │       │
│  │  1. Shuffle images (with random_seed for reproducibility)│       │
│  │  2. Calculate split sizes:                               │       │
│  │                                                           │       │
│  │     Total images: N                                       │       │
│  │     Hold-out:  10% of N  → H images                      │       │
│  │     Remaining: 90% of N  → R images                      │       │
│  │                                                           │       │
│  │     Train:     70% of R  → T images                      │       │
│  │     Val:       15% of R  → V images                      │       │
│  │     Test:      15% of R  → S images                      │       │
│  │                                                           │       │
│  │  3. Assign split labels to each image                    │       │
│  └──────────────────────────────────────────────────────────┘       │
│                              │                                       │
│                              ▼                                       │
│  ┌──────────────────────────────────────────────────────────┐       │
│  │  C. Stratification Guarantees                             │       │
│  │  ──────────────────────────                               │       │
│  │  • Each class appears in all splits                       │       │
│  │  • Class proportions maintained across splits             │       │
│  │  • Similar images (same cluster) stay together           │       │
│  │  • Hold-out set reserved from different domains          │       │
│  └──────────────────────────────────────────────────────────┘       │
│                                                                       │
│  Output: Each image assigned to train/val/test/holdout              │
└─────────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│  STEP 5: CREATE OUTPUT STRUCTURE                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                       │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐            │
│  │  Create     │───▶│  Copy       │───▶│  Update     │            │
│  │  Folders    │    │  Images     │    │  Paths      │            │
│  └─────────────┘    └─────────────┘    └─────────────┘            │
│                                                                       │
│  Directory Structure Created:                                        │
│                                                                       │
│  merged_dataset/                                                     │
│  ├── images/                                                         │
│  │   ├── train/                                                      │
│  │   │   ├── class_a/                                               │
│  │   │   │   ├── dataset1_image001.jpg                              │
│  │   │   │   ├── dataset2_image002.jpg                              │
│  │   │   │   └── ...                                                │
│  │   │   ├── class_b/                                               │
│  │   │   └── ...                                                    │
│  │   ├── val/                                                        │
│  │   │   └── [same structure]                                       │
│  │   ├── test/                                                       │
│  │   │   └── [same structure]                                       │
│  │   └── holdout/                                                    │
│  │       └── [same structure]                                       │
│  ├── metadata/                                                       │
│  │   └── [CSV files - created in next step]                        │
│  └── reports/                                                        │
│      └── [Report files - created in next step]                     │
│                                                                       │
│  File Naming Convention:                                             │
│  {dataset_source}_{original_stem}.{extension}                       │
│  Example: RiceDataset_leaf_001.jpg                                  │
│                                                                       │
│  Output: Organized image files in split/class hierarchy             │
└─────────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│  STEP 6: GENERATE METADATA FILES                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                       │
│  ┌───────────────────────────────────────────────────────┐          │
│  │  1. merged_metadata.csv                                │          │
│  │  ───────────────────────                               │          │
│  │  Complete metadata for ALL images                     │          │
│  │  (including duplicates)                                │          │
│  │                                                        │          │
│  │  Columns: image_id, original_path, filename,          │          │
│  │          dataset_source, canonical_class,              │          │
│  │          width, height, resolution, format,            │          │
│  │          md5_hash, perceptual_hash, quality_score,     │          │
│  │          provenance, is_duplicate, cluster_id, split   │          │
│  └───────────────────────────────────────────────────────┘          │
│                              │                                       │
│  ┌───────────────────────────────────────────────────────┐          │
│  │  2. provenance_map.csv                                 │          │
│  │  ──────────────────────                                │          │
│  │  Detailed provenance tracking                          │          │
│  │  One row per source (handles merged duplicates)       │          │
│  │                                                        │          │
│  │  Columns: image_id, canonical_class,                  │          │
│  │          dataset_source, original_path,                │          │
│  │          original_filename, scan_date                  │          │
│  └───────────────────────────────────────────────────────┘          │
│                              │                                       │
│  ┌───────────────────────────────────────────────────────┐          │
│  │  3. similarity_clusters.csv                            │          │
│  │  ───────────────────────────                           │          │
│  │  Cluster assignments for similar images               │          │
│  │                                                        │          │
│  │  Columns: image_id, filename, canonical_class,        │          │
│  │          cluster_id, perceptual_hash                   │          │
│  └───────────────────────────────────────────────────────┘          │
│                              │                                       │
│  ┌───────────────────────────────────────────────────────┐          │
│  │  4. split_files_list.csv                               │          │
│  │  ────────────────────────                              │          │
│  │  Train/val/test/holdout assignments                    │          │
│  │  (unique images only)                                  │          │
│  │                                                        │          │
│  │  Columns: image_id, filename, canonical_class,        │          │
│  │          split, merged_path, dataset_source            │          │
│  └───────────────────────────────────────────────────────┘          │
│                              │                                       │
│  ┌───────────────────────────────────────────────────────┐          │
│  │  5. duplicates_report.csv                              │          │
│  │  ─────────────────────────                             │          │
│  │  Detailed duplicate analysis                           │          │
│  │                                                        │          │
│  │  Columns: primary_image_id, primary_filename,         │          │
│  │          duplicate_image_id, duplicate_filename,       │          │
│  │          quality_scores, md5_hash, perceptual_hash     │          │
│  └───────────────────────────────────────────────────────┘          │
│                                                                       │
│  Output: 5 comprehensive CSV files                                  │
└─────────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│  STEP 7: GENERATE SUMMARY REPORT                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                       │
│  merge_summary_report.txt                                            │
│  ─────────────────────────                                           │
│                                                                       │
│  📊 OVERVIEW                                                         │
│     • Datasets processed: N                                          │
│     • Total images scanned: X                                        │
│     • Duplicates removed: Y                                          │
│     • Unique images: Z                                               │
│     • Classes identified: C                                          │
│                                                                       │
│  📁 DATASETS PROCESSED                                               │
│     • List of all source datasets                                    │
│                                                                       │
│  🏷️  CLASSES FOUND                                                   │
│     • Alphabetical list of all classes                               │
│                                                                       │
│  📈 SPLIT DISTRIBUTION                                               │
│     For each split (train/val/test/holdout):                        │
│     • Total count                                                    │
│     • Per-class breakdown                                            │
│                                                                       │
│  📄 OUTPUT FILES GENERATED                                           │
│     • Directory structure                                            │
│     • File descriptions                                              │
│                                                                       │
│  Output: Human-readable summary report                              │
└─────────────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────────────┐
│                    ✅ MERGE COMPLETE!                                │
│                                                                       │
│  Final Deliverables:                                                 │
│  ├── Organized image folder hierarchy                               │
│  ├── Complete metadata tables (5 CSV files)                         │
│  ├── Duplicate analysis report                                       │
│  └── Comprehensive summary                                           │
│                                                                       │
│  Ready for:                                                          │
│  ✓ Data augmentation                                                │
│  ✓ Model training                                                    │
│  ✓ Quality validation                                                │
│  ✓ Further curation                                                  │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔍 Detailed Component Diagrams

### Deduplication Algorithm

```
┌────────────────────────────────────────────────────────────┐
│         DEDUPLICATION DECISION TREE                        │
└────────────────────────────────────────────────────────────┘

For each image:
│
├─ Compute MD5 hash
│  └─ Add to md5_hash_map[hash]
│
└─ Compute Perceptual hash (pHash)
   └─ Add to phash_map[hash]

After scanning all images:
│
├─ Find Exact Duplicates
│  │
│  ├─ For each MD5 hash with multiple images:
│  │  │
│  │  ├─ Sort group by quality_score (descending)
│  │  │
│  │  ├─ PRIMARY = highest quality
│  │  │  └─ Merge all provenance → PRIMARY
│  │  │
│  │  └─ DUPLICATES = rest
│  │     └─ Set is_duplicate=True, duplicate_of=PRIMARY
│  │
│  └─ Result: Exact duplicate groups identified
│
└─ Find Near Duplicates
   │
   ├─ For each pHash with multiple images:
   │  (excluding already-processed exact duplicates)
   │  │
   │  ├─ Sort group by quality_score (descending)
   │  │
   │  ├─ PRIMARY = highest quality
   │  │  └─ Merge all provenance → PRIMARY
   │  │
   │  └─ DUPLICATES = rest
   │     └─ Set is_duplicate=True, duplicate_of=PRIMARY
   │
   └─ Result: Near duplicate groups identified

Quality Score Calculation:
┌────────────────────────────────────────┐
│  grayscale = convert_to_gray(image)    │
│  laplacian = Laplacian(grayscale)      │
│  variance = var(laplacian)             │
│  quality = min(100, variance / 10)     │
└────────────────────────────────────────┘

Higher variance = Sharper edges = Better quality
```

### Similarity Clustering Process

```
┌────────────────────────────────────────────────────────────┐
│         SIMILARITY CLUSTERING WORKFLOW                     │
└────────────────────────────────────────────────────────────┘

Input: Unique images (non-duplicates)
│
├─ Extract Perceptual Hashes
│  └─ Convert to ImageHash objects
│
├─ Compute Distance Matrix
│  │
│  │  For i, j in all image pairs:
│  │    distance[i][j] = HammingDistance(phash[i], phash[j])
│  │
│  └─ Result: N×N symmetric matrix
│
├─ Apply DBSCAN Clustering
│  │
│  │  Parameters:
│  │  • eps = 5.0 (max Hamming distance)
│  │  • min_samples = 2 (min cluster size)
│  │  • metric = 'precomputed'
│  │
│  └─ Algorithm identifies dense regions
│
└─ Assign Cluster Labels
   │
   ├─ cluster_id ≥ 0: Image belongs to cluster
   │  └─ Similar images (e.g., same plant photographed multiple times)
   │
   └─ cluster_id = -1: Noise (unique images)
      └─ No similar images found

Why This Matters:
┌────────────────────────────────────────────────────────────┐
│  Without clustering:                                       │
│  Train: [leaf_1_view_a, leaf_2_view_a]                    │
│  Test:  [leaf_1_view_b, leaf_2_view_b]                    │
│  → Model memorizes specific leaves! (Data leakage)        │
│                                                            │
│  With clustering:                                          │
│  Train: [leaf_1_view_a, leaf_1_view_b]                    │
│  Test:  [leaf_2_view_a, leaf_2_view_b]                    │
│  → Model generalizes to new leaves! (Proper validation)   │
└────────────────────────────────────────────────────────────┘
```

### Split Strategy

```
┌────────────────────────────────────────────────────────────┐
│         STRATIFIED SPLIT STRATEGY                          │
└────────────────────────────────────────────────────────────┘

Example: Class "Early Blight" with 1000 images

Step 1: Reserve Hold-out (10%)
├─ Hold-out: 100 images (10% of 1000)
└─ Remaining: 900 images

Step 2: Split Remaining (900 images)
├─ Train:      630 images (70% of 900)
├─ Validation: 135 images (15% of 900)
└─ Test:       135 images (15% of 900)

Final Distribution:
┌──────────────┬────────┬────────────┐
│ Split        │ Count  │ Percentage │
├──────────────┼────────┼────────────┤
│ Train        │ 630    │ 63.0%      │
│ Validation   │ 135    │ 13.5%      │
│ Test         │ 135    │ 13.5%      │
│ Hold-out     │ 100    │ 10.0%      │
├──────────────┼────────┼────────────┤
│ TOTAL        │ 1000   │ 100.0%     │
└──────────────┴────────┴────────────┘

Stratification Guarantee:
Each class maintains the same proportions across splits.

Example with 3 classes:
┌───────────────┬───────┬─────┬──────┬──────┬─────────┐
│ Class         │ Total │ Trn │ Val  │ Test │ Holdout │
├───────────────┼───────┼─────┼──────┼──────┼─────────┤
│ Healthy       │ 5000  │ 63% │ 13.5%│ 13.5%│ 10%     │
│ Early Blight  │ 3000  │ 63% │ 13.5%│ 13.5%│ 10%     │
│ Late Blight   │ 2000  │ 63% │ 13.5%│ 13.5%│ 10%     │
└───────────────┴───────┴─────┴──────┴──────┴─────────┘

Hold-out Set Purpose:
┌────────────────────────────────────────────────────────────┐
│  • Completely untouched during development                 │
│  • Used ONLY for final model evaluation                    │
│  • Simulates real-world deployment performance             │
│  • Ideally from different sources/domains than training    │
└────────────────────────────────────────────────────────────┘
```

## 📈 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     DATA TRANSFORMATIONS                        │
└─────────────────────────────────────────────────────────────────┘

Raw Datasets
   │
   │ (Multiple folders with varying structures)
   │
   ▼
┌─────────────────┐
│ Image Registry  │  ← List of all images with metadata
│ (List[Dict])    │
└─────────────────┘
   │
   │ (Compute hashes, extract metadata)
   │
   ▼
┌─────────────────────────────────────┐
│ Hash Maps                           │
│ • md5_hash_map: {hash: [indices]}  │
│ • phash_map: {hash: [indices]}     │
└─────────────────────────────────────┘
   │
   │ (Deduplication)
   │
   ▼
┌─────────────────────────────────────┐
│ Updated Image Registry              │
│ • Primary images: is_duplicate=False│
│ • Duplicates: is_duplicate=True     │
│ • Merged provenance                 │
└─────────────────────────────────────┘
   │
   │ (Clustering)
   │
   ▼
┌─────────────────────────────────────┐
│ Image Registry + Cluster IDs        │
│ • cluster_id assigned               │
│ • Similar images grouped            │
└─────────────────────────────────────┘
   │
   │ (Splitting)
   │
   ▼
┌─────────────────────────────────────┐
│ Image Registry + Split Labels       │
│ • split: train/val/test/holdout     │
│ • Stratified by class               │
└─────────────────────────────────────┘
   │
   │ (File organization)
   │
   ▼
┌─────────────────────────────────────┐
│ Organized File Structure            │
│ + Updated paths in registry         │
└─────────────────────────────────────┘
   │
   │ (Export)
   │
   ▼
┌──────────────────────────────────────────────────┐
│ Final Outputs                                    │
│ ├── images/ (organized folders)                 │
│ ├── metadata/ (5 CSV files)                     │
│ └── reports/ (2 report files)                   │
└──────────────────────────────────────────────────┘
```

## 🎯 Quality Assurance Workflow

```
┌────────────────────────────────────────────────────────────┐
│            QUALITY CHECKS & VALIDATION                     │
└────────────────────────────────────────────────────────────┘

After Merging:
│
├─ 1. Check Summary Report
│  ├─ Total images scanned vs. expected
│  ├─ Number of duplicates (reasonable?)
│  ├─ Classes found (all expected classes?)
│  └─ Split distributions (balanced?)
│
├─ 2. Validate Metadata
│  ├─ Load merged_metadata.csv
│  ├─ Check for NULL values
│  ├─ Verify hash uniqueness (primary images)
│  └─ Inspect quality_score distribution
│
├─ 3. Review Duplicates
│  ├─ Load duplicates_report.csv
│  ├─ Manually inspect a sample
│  ├─ Verify quality-based selection is correct
│  └─ Check provenance was merged properly
│
├─ 4. Inspect Clusters
│  ├─ Load similarity_clusters.csv
│  ├─ Visualize sample clusters
│  ├─ Verify images in cluster are truly similar
│  └─ Check cluster sizes (not too large/small)
│
├─ 5. Validate Splits
│  ├─ Load split_files_list.csv
│  ├─ Verify class balance across splits
│  ├─ Check hold-out set is from diverse sources
│  ├─ Ensure no cluster spans multiple splits
│  └─ Confirm reproducibility (same seed = same splits)
│
└─ 6. Visual Inspection
   ├─ Sample random images from each split
   ├─ Verify images are correctly labeled
   ├─ Check image quality is acceptable
   └─ Look for mislabeled or corrupted images

Decision Tree:
┌────────────────────────────────────────────────────┐
│  All checks pass?                                  │
│  ├─ YES → Proceed with training                   │
│  └─ NO → Review issues:                           │
│      ├─ Missing classes → Check original datasets │
│      ├─ Imbalanced splits → Adjust ratios         │
│      ├─ Bad duplicates → Tune similarity threshold│
│      └─ Wrong labels → Fix source data            │
└────────────────────────────────────────────────────┘
```

---

**End of Visual Workflow**  
For detailed usage instructions, see `DATASET_MERGER_GUIDE.md`
