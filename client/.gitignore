# Client-specific overrides
# (Most rules are handled by root .gitignore)

# Vite development files
.vite/
dist-ssr/

# Local environment overrides
.env.development.local
.env.test.local
.env.production.local
